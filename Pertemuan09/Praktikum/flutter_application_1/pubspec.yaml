name: flutter_application_2
description: A new Flutter project.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  intl: ^0.19.0
  file_picker: ^8.0.0   # ← versi aman & terbaru
  cupertino_icons: ^1.0.6
  flutter_colorpicker: ^1.1.0
  open_file: ^3.5.10
  shared_preferences: ^2.5.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
